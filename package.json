{
  "name": "blinking",
  "version": "0.0.4",
  "description": "A module for \"blinking\" to a file (useful for writing to an LED file for actual blinks)",
  "main": "index.js",
  "scripts": {
    "prepare": "npx tsc",
    "lint": "balena-lint --typescript --fix index.ts",
    "test": "npm run lint && npm run prepare"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:balena-io-modules/blinking.git"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/bluebird": "^3.5.33",
    "bluebird": "^3.0.0"
  },
  "devDependencies": {
    "@balena/lint": "^5.4.1",
    "typescript": "^4.2.4"
  }
}
